_target_: mmtsfm.data.datamodule.MMTSFMDataModule
data_dir: /leonardo_scratch/fast/IscrC_MTSFM/data
h5_path: null
dataset_name: uk_pv
batch_size: 16
num_workers: 8
num_entities: 4

# Dataset-of-record defaults (BASELINE_PROTOCOL.md): 14-day history / 6-hour
# horizon, resolved per dataset cadence by PVRecordDataset.
history_days: 14.0
horizon_hours: 6.0
hist_steps: null
horizon: 12
target_dim: 1
covariate_dim: 14
video_frames: 8
visual_window_hours: 6.0
img_size: 224
img_channels: 3
imagenet_norm: true
vjepa_cache_dir: null
num_samples_train: 1000
num_samples_val: 200
num_samples_test: 200
train_frac: 0.70
val_frac: 0.10
vis_cadence_multiplier: 1  # >1 queries frames at M× denser cadence than TS steps (C4 decoupling)
