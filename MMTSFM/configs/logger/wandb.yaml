_target_: lightning.pytorch.loggers.wandb.WandbLogger
project: ${project_name}
name: ${run_name}
save_dir: ${hydra:run.dir}
offline: True  # compute nodes have no internet; sync with `wandb sync` post-job
log_model: False  # model uploads require cloud; use checkpoint callbacks instead
job_type: "train"
